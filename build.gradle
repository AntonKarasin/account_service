plugins {
    id 'java'
}

group = 'com.beprog'
version = '0.0.1-SNAPSHOT'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

repositories {
    mavenCentral()
}

dependencies {
    // todo: добавить необходимые зависимости для postgres
}

tasks.named('test') {
    useJUnitPlatform()
}
